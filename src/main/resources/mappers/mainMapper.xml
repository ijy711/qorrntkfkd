<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
 
<mapper namespace="com.home.work.mapper.MainMapper">
 
<!--     <select id="testList" resultType="com.home.work.domain.MainVO">
        select * from s_score
    </select> -->
    
    <select id="tableList" parameterType="com.home.work.domain.MainVO" resultType="com.home.work.domain.MainVO">
    <![CDATA[
SELECT CD_NM||NVL2(REGION_AREA,'=>'||REGION_AREA,'합계') NM
      ,COL1||'('||TRUNC((COL1/TOT*100),2)||'%)'||(DECODE(COL1, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL1,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK1 COL1
      ,COL2||'('||TRUNC((COL2/TOT*100),2)||'%)'||(DECODE(COL2, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL2,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK2 COL2
      ,COL3||'('||TRUNC((COL3/TOT*100),2)||'%)'||(DECODE(COL3, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL3,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK3 COL3
      ,COL4||'('||TRUNC((COL4/TOT*100),2)||'%)'||(DECODE(COL4, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL4,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK4 COL4
      ,COL5||'('||TRUNC((COL5/TOT*100),2)||'%)'||(DECODE(COL5, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL5,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK5 COL5
      ,COL6||'('||TRUNC((COL6/TOT*100),2)||'%)'||(DECODE(COL6, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL6,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK6 COL6
      ,COL7||'('||TRUNC((COL7/TOT*100),2)||'%)'||(DECODE(COL7, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL7,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK7 COL7
      ,COL8||'('||TRUNC((COL8/TOT*100),2)||'%)'||(DECODE(COL8, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL8,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK8 COL8
      ,COL9||'('||TRUNC((COL9/TOT*100),2)||'%)'||(DECODE(COL9, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL9,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK9 COL9
      ,COL10||'('||TRUNC((COL10/TOT*100),2)||'%)'||(DECODE(COL10, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
            ||DECODE(COL10,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||'*'||RK10 COL10
      ,TOT
FROM (
    SELECT AREA_CD, REGION_AREA
        ,MIN(DECODE(PROD_ID,'00001',CCT)) COL1
        ,MIN(DECODE(PROD_ID,'00002',CCT)) COL2
        ,MIN(DECODE(PROD_ID,'00003',CCT)) COL3
        ,MIN(DECODE(PROD_ID,'00004',CCT)) COL4
        ,MIN(DECODE(PROD_ID,'00005',CCT)) COL5
        ,MIN(DECODE(PROD_ID,'00006',CCT)) COL6
        ,MIN(DECODE(PROD_ID,'00007',CCT)) COL7
        ,MIN(DECODE(PROD_ID,'00008',CCT)) COL8
        ,MIN(DECODE(PROD_ID,'00009',CCT)) COL9
        ,MIN(DECODE(PROD_ID,'00010',CCT)) COL10
        ,MIN(DECODE(PROD_ID,'00001',RK)) RK1
        ,MIN(DECODE(PROD_ID,'00002',RK)) RK2
        ,MIN(DECODE(PROD_ID,'00003',RK)) RK3
        ,MIN(DECODE(PROD_ID,'00004',RK)) RK4
        ,MIN(DECODE(PROD_ID,'00005',RK)) RK5
        ,MIN(DECODE(PROD_ID,'00006',RK)) RK6
        ,MIN(DECODE(PROD_ID,'00007',RK)) RK7
        ,MIN(DECODE(PROD_ID,'00008',RK)) RK8
        ,MIN(DECODE(PROD_ID,'00009',RK)) RK9
        ,MIN(DECODE(PROD_ID,'00010',RK)) RK10
        ,SUM(CCT) TOT
    FROM(
        SELECT AREA_CD
              ,REGION_AREA
              ,PROD_ID
              ,SUM(SALES_CNT) CCT
              ,RANK() OVER(PARTITION BY AREA_CD,REGION_AREA ORDER BY SUM(SALES_CNT) DESC) RK
        FROM SALE_TBL
        WHERE ROWNUM <100000
        GROUP BY AREA_CD,REGION_AREA,PROD_ID
        )
    GROUP BY ROLLUP (AREA_CD,REGION_AREA)
    ), CD_TBL
WHERE AREA_CD=CD_ID(+)
    ]]>
    </select>
    
    <select id="lastTest" parameterType="java.util.Map" resultType="com.home.work.domain.InputVO">
    	<![CDATA[
    	SELECT CD_NM||NVL2(REGION_AREA,'=>'||REGION_AREA,'합계') NM
      ,COL1||'('||TRUNC((COL1/TOT*100),2)||'%)'||(DECODE(COL1, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL1,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK1) COL1
      ,COL2||'('||TRUNC((COL2/TOT*100),2)||'%)'||(DECODE(COL2, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL2,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK2) COL2
      ,COL3||'('||TRUNC((COL3/TOT*100),2)||'%)'||(DECODE(COL3, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL3,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK3) COL3
      ,COL4||'('||TRUNC((COL4/TOT*100),2)||'%)'||(DECODE(COL4, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL4,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK4) COL4
      ,COL5||'('||TRUNC((COL5/TOT*100),2)||'%)'||(DECODE(COL5, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL5,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK5) COL5
      ,COL6||'('||TRUNC((COL6/TOT*100),2)||'%)'||(DECODE(COL6, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL6,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK6) COL6
      ,COL7||'('||TRUNC((COL7/TOT*100),2)||'%)'||(DECODE(COL7, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL7,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK7) COL7
      ,COL8||'('||TRUNC((COL8/TOT*100),2)||'%)'||(DECODE(COL8, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL8,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK8) COL8
      ,COL9||'('||TRUNC((COL9/TOT*100),2)||'%)'||(DECODE(COL9, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
           ||DECODE(COL9,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK9) COL9
      ,COL10||'('||TRUNC((COL10/TOT*100),2)||'%)'||(DECODE(COL10, GREATEST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10), '▲')
            ||DECODE(COL10,LEAST(COL1, COL2, COL3, COL4, COL5, COL6, COL7, COL8, COL9, COL10),'▼'))||DECODE(ZO,0,'*'||RK10) COL10
      ,TOT
FROM (
    SELECT AREA_CD, REGION_AREA
        ,SUM(DECODE(PROD_ID,'00001',CCT)) COL1
        ,SUM(DECODE(PROD_ID,'00002',CCT)) COL2
        ,SUM(DECODE(PROD_ID,'00003',CCT)) COL3
        ,SUM(DECODE(PROD_ID,'00004',CCT)) COL4
        ,SUM(DECODE(PROD_ID,'00005',CCT)) COL5
        ,SUM(DECODE(PROD_ID,'00006',CCT)) COL6
        ,SUM(DECODE(PROD_ID,'00007',CCT)) COL7
        ,SUM(DECODE(PROD_ID,'00008',CCT)) COL8
        ,SUM(DECODE(PROD_ID,'00009',CCT)) COL9
        ,SUM(DECODE(PROD_ID,'00010',CCT)) COL10
        ,SUM(DECODE(PROD_ID,'00001',RK)) RK1
        ,SUM(DECODE(PROD_ID,'00002',RK)) RK2
        ,SUM(DECODE(PROD_ID,'00003',RK)) RK3
        ,SUM(DECODE(PROD_ID,'00004',RK)) RK4
        ,SUM(DECODE(PROD_ID,'00005',RK)) RK5
        ,SUM(DECODE(PROD_ID,'00006',RK)) RK6
        ,SUM(DECODE(PROD_ID,'00007',RK)) RK7
        ,SUM(DECODE(PROD_ID,'00008',RK)) RK8
        ,SUM(DECODE(PROD_ID,'00009',RK)) RK9
        ,SUM(DECODE(PROD_ID,'00010',RK)) RK10
        ,SUM(CCT) TOT
        ,(GROUPING(AREA_CD)||GROUPING(REGION_AREA))*DECODE(#{dir,jdbcType=VARCHAR},'F',1,-1) ZO
        ,DECODE(AREA_CD,'10',#{ord1,jdbcType=VARCHAR},1,'20',#{ord2,jdbcType=VARCHAR},'30',#{ord3,jdbcType=VARCHAR},'40',#{ord4,jdbcType=VARCHAR},'60',#{ord6,jdbcType=VARCHAR},'70',#{ord7,jdbcType=VARCHAR}) ORD
    FROM(
        SELECT AREA_CD
              ,REGION_AREA
              ,PROD_ID
              ,SUM(SALES_CNT) CCT
              ,RANK() OVER(PARTITION BY AREA_CD,REGION_AREA ORDER BY SUM(SALES_CNT) DESC) RK
        FROM SALE_TBL
        WHERE ROWNUM <#{cnt,jdbcType=VARCHAR} AND AREA_CD=NVL(#{location,jdbcType=VARCHAR},AREA_CD)
        GROUP BY AREA_CD,REGION_AREA,PROD_ID
        )
    GROUP BY ROLLUP (AREA_CD,REGION_AREA)
    ), CD_TBL
WHERE AREA_CD=CD_ID(+) AND ZO IN (DECODE(NVL(#{lvl,jdbcType=VARCHAR},1),1,ZO,2,'0',3,'1'),DECODE(#{lvl,jdbcType=VARCHAR},3,'11'))
ORDER BY NVL(ORD,RPAD(SUBSTR(AREA_CD,1,1),1,0)),ZO
    	]]>
    </select>
 
</mapper>
